{
  "name": "ms-authentication",
  "version": "1.0.0",
  "description": "Micro-service de login",
  "main": "./build/index.js",
  "scripts": {
    "start": "node ./",
    "build": "tsc -p .",
    "dev": "ts-node-dev --respawn --transpile-only --ignore-watch node_modules --no-notify src/index.ts",
    "watcher": "npx tsc --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Lucas Santos",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.13",
    "@types/node": "^18.6.3",
    "ts-node-dev": "^2.0.0",
    "typescript": "^4.7.4"
  },
  "dependencies": {
    "express": "^4.18.1"
  }
}
